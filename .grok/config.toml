# Grok Bot / Grok CLI — project MCP (merged when you run grok from this repo).
# Public remote Streamable HTTP — no auth. Verify: grok mcp doctor picktherobot

[mcp_servers.picktherobot]
url = "https://picktherobot.com/api/mcp"
